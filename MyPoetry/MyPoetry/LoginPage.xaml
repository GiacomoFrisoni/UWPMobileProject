<Page
    x:Class="MyPoetry.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyPoetry"
    xmlns:custom="using:MyPoetry.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Image Stretch="UniformToFill" Source="Assets/background.png"/>
        <StackPanel VerticalAlignment="Center" Margin="0, -50, 0, 0" Padding="50" MaxWidth="400">
            <StackPanel.ChildrenTransitions>
                <TransitionCollection>
                    <ContentThemeTransition></ContentThemeTransition>
                </TransitionCollection>
            </StackPanel.ChildrenTransitions>

            <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" x:Uid="Login" Text="Login" Margin="5"></TextBlock>

            <TextBox Margin="5" x:Uid="Email" PlaceholderText="Email"></TextBox>
            <TextBox Margin="5" x:Uid="Password" PlaceholderText="Password"></TextBox>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <CheckBox Margin="5" x:Uid="StayLogged" Content="Stay logged"></CheckBox>
                <Button Grid.Column="1" Background="{StaticResource AppColor1}" Foreground="White" Content="Login" Margin="5" HorizontalAlignment="Stretch"></Button>
            </Grid>

            <Button x:Uid="ForgetPassword" Background="Transparent" BorderBrush="Transparent" Content="Forget password?" HorizontalAlignment="Center" Margin="10"></Button>

            <Button x:Uid="Register" Margin="0, 80, 0, 0" BorderBrush="{StaticResource AppColor1}" Background="White" Content="Register now" HorizontalAlignment="Center" Width="250"></Button>
        </StackPanel>
    </Grid>
</Page>
