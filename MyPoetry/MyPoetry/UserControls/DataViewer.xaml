<UserControl
    x:Class="MyPoetry.UserControls.DataViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyPoetry.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="60"
    d:DesignWidth="400">

    <Grid Background="{StaticResource AppColor1}" HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Viewbox Margin="10">
            <SymbolIcon x:Name="Symbol" Foreground="White" Symbol="{Binding Icon}"></SymbolIcon>
        </Viewbox>

        <Grid Background="{StaticResource AppColor4}" Padding="10, 5" Grid.Column="1">
            <StackPanel>
                <TextBlock Style="{StaticResource BodyTextBlockStyle}" FontWeight="SemiLight"  Text="{Binding Description}"></TextBlock>
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" FontWeight="SemiLight"  TextWrapping="NoWrap" Text="{Binding Value}"></TextBlock>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
