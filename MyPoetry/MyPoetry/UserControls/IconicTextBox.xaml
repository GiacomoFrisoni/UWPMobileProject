<UserControl
    x:Class="MyPoetry.UserControls.IconicTextBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyPoetry.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="40"
    d:DesignWidth="400">
    <UserControl.Resources>

    </UserControl.Resources>

    <Border BorderBrush="{StaticResource TextBoxBorderThemeBrush}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0"></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Rectangle Fill="LightBlue"></Rectangle>
            <SymbolIcon Name="icon" Foreground="White" Width="20" Height="20"></SymbolIcon>
            <TextBox Grid.Column="1" Name="text" BorderBrush="Transparent"></TextBox>
        </Grid>
    </Border>
</UserControl>