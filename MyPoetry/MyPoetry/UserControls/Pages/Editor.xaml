<UserControl
    x:Class="MyPoetry.UserControls.Pages.Editor"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyPoetry.UserControls.Pages"
    xmlns:custom="using:MyPoetry.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <custom:CustomPage x:Name="MainContent" Title="Editor">
        <custom:CustomPage.TopButtons>
            <StackPanel>
                <Button x:Name="BtnSave" Height="48" Width="70" Style="{StaticResource ButtonBlue}" Click="BtnSave_Click">
                    <SymbolIcon Symbol="Save"></SymbolIcon>
                </Button>
            </StackPanel>
        </custom:CustomPage.TopButtons>
        
        <custom:CustomPage.MainContent>
            <Grid x:Name="GrdParent">
                <SplitView x:Name="SpvContent" DisplayMode="Overlay" PanePlacement="Right">
                    <SplitView.Content>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="10">
                                <TextBox x:Name="TxbTitle" FontSize="20" Padding="10" BorderBrush="Transparent" BorderThickness="0" Background="#44FFFFFF" PlaceholderText="Titolo" KeyUp="TxbTitle_KeyUp"></TextBox>
                            </Grid>

                            <Grid Grid.Row="1" Margin="10, 5, 10, 10">
                                <RichEditBox Padding="10" x:Name="RebText" BorderBrush="Transparent" BorderThickness="0" Background="#44FFFFFF" PlaceholderText="Testo" SelectionChanged="RebText_SelectionChanged"></RichEditBox>
                            </Grid>

                            <Grid Height="48" Grid.Row="2" Background="{StaticResource AppColor1}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition Width="Auto"></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="10, 0">
                                    <Button Height="48" Style="{StaticResource ButtonBlue}" Click="Button_Click">
                                        <SymbolIcon Symbol="Font"></SymbolIcon>
                                    </Button>
                                    <Button Height="48" Style="{StaticResource ButtonBlue}">
                                        <SymbolIcon Symbol="Bookmarks"></SymbolIcon>
                                    </Button>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right" Margin="10, 0">
                                    <ToggleButton x:Name="BtnBold" Height="48" Style="{StaticResource CustomToggleButton}" Click="BtnBold_Click">
                                        <SymbolIcon Symbol="Bold"></SymbolIcon>
                                    </ToggleButton>
                                    <ToggleButton x:Name="BtnItalic" Height="48" Style="{StaticResource CustomToggleButton}" Click="BtnItalic_Click">
                                        <SymbolIcon Symbol="Italic"></SymbolIcon>
                                    </ToggleButton>
                                    <ToggleButton x:Name="BtnUnderline" Height="48" Style="{StaticResource CustomToggleButton}" Click="BtnUnderline_Click">
                                        <SymbolIcon Symbol="Underline"></SymbolIcon>
                                    </ToggleButton>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </SplitView.Content>

                    <SplitView.Pane>
                        <Grid>
                            <TextBlock>Sample text</TextBlock>
                        </Grid>
                    </SplitView.Pane>
                </SplitView>

                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroup>

                        <VisualState x:Name="Large">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="700" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>
                                <Setter Target="SpvContent.DisplayMode"
                                Value="Inline" />
                                <Setter Target="SpvContent.IsPaneOpen"
                                Value="True" />
                            </VisualState.Setters>
                        </VisualState>


                        <VisualState x:Name="Small">
                            <VisualState.StateTriggers>
                                <AdaptiveTrigger MinWindowWidth="0" />
                            </VisualState.StateTriggers>
                            <VisualState.Setters>

                                <Setter Target="SpvContent.DisplayMode"
                                Value="Overlay" />
                                <Setter Target="SpvContent.IsPaneOpen"
                                Value="False" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateManager.VisualStateGroups>
            </Grid>
        </custom:CustomPage.MainContent>

    </custom:CustomPage>
</UserControl>
